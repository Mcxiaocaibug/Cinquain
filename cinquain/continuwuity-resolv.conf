# Avoid Docker's embedded resolver bottleneck during Matrix federation.
# Cloudflare is used as a predictable fallback; replace these entries if your
# environment requires an internal validating resolver.
nameserver 1.1.1.1
nameserver 1.0.0.1
options timeout:2 attempts:3 rotate
